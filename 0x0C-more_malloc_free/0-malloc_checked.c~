#include <stdlib.h>
#include "main.h"
/**
 * malloc_checked - allocates a memory
 * @b: unsigned int
 * Return: void pointer
 */
void *malloc_checked(unsigned int b)
{
int *p;
p = malloc(b);
if (p == '\0')
{
exit(98);
}
return (p);
}
